export const metadata = {
    title: "Generate URLs | QuickURL",
    description: "Generate short URLs using QuickURL",
}

export default function ShortenLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
